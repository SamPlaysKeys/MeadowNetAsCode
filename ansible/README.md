# MeadowNetAsCode - Ansible

## Secrets Management

Currently, the Ansible vault file is stored manually on the self-hosted runner at `/tmp/vault.yml`. 

**Note:** The values in this file will eventually be migrated to the local deployment of HashiCorp Vault.
